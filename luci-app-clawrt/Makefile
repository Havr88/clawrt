include $(TOPDIR)/rules.mk

LUCI_TITLE:=LuCI support for ClawRT AI Router Agent
LUCI_DEPENDS:=+clawrt
LUCI_PKGARCH:=all

PKG_NAME:=luci-app-clawrt
PKG_VERSION:=2.0.0
PKG_RELEASE:=1

include $(TOPDIR)/feeds/luci/luci.mk

# call BuildPackage - OpenWrt buildroot signature
